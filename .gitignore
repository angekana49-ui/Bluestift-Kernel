__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
# .env.example is deliberately NOT ignored: it holds no secrets and is the only
# tracked record of what a deployment needs. The README's setup step copies it.

# Local corporate-proxy CA bundle (machine-specific, not for the repo)
.ca-bundle.pem

# Generated graph visualization (regenerate with scripts/visualize_graph.py)
graph_viz.html
