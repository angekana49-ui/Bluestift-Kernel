# Test tooling. Not installed in the deployed image — install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.0.3
pytest-asyncio==1.4.0   # 0.25 caps pytest <9; a clean install could not resolve
